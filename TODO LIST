(x) Validate check mate
  (x)  0) is check
  (x)  1) king can't scape
  (x)  2) can't capture the atacking piece
  (x)  3) can't place a piece between the atacking piece and the king 

()there is a bug when a pawn captures in the second rank
(x) allow castle
   (x) short castle
   (x) long castle
   (x) Neither the king nor the chosen rook has previously moved.
   (x) There are no pieces between the king and the chosen rook.
   (x) The king is not currently in check.
   (x) The king does not pass through a square that is attacked by an enemy piece.
   () The king does not end up in check. (True of any legal move.)

   (x) swap rook on castle

(x) setup position from fen

() promotion

() Page to solve mates
    (X) copy position from fen
    () it finds the checkmate but not the optimal one
    () Panel with moves list
      (X) hardcoded list
        (X)select current move
        (X)next button behaviour
        () previous button behaviour
      () real list

() Validate check mates: mate del pasillo, 
  (x) mates donde se necesite la función isOnXRay

